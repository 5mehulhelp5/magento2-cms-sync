# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0
greenlet==3.1.1

# Configuration
python-dotenv==1.0.1

# Validation
pydantic==2.10.0
pydantic-settings==2.6.1

# HTTP Client
httpx==0.28.0

# File Upload
python-multipart==0.0.18

# Async File I/O (CRITICAL FIX for blocking I/O)
aiofiles==23.2.1

# Note: Removed unused dependencies
# - python-jose[cryptography] (not used anywhere)
# - passlib[bcrypt] (not used anywhere)
